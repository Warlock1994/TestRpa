- 更新任何内容都不要专门创建一个更新总结Markdown文档！
- 我让你加的或更新的任何功能你都必须自己完整自测过一遍！确保肯定没有任何问题再让我测试！
- 彻底完成一次更新后必须启动一遍项目，确保至少启动项目时不会报错！
- 必须用中文回复我
- 开发或更新过程中必须考虑到项目的可扩展性和可维护性！
- 项目必须尽可能的拆分模块，不要把大量代码都写在同一个代码文件中！
- 项目采用分层架构
- 项目中任何可能导致软件崩溃的代码都必须加上异常处理机制！不能因为一个报错，导致整个软件都崩溃了！
- 项目中可能导致主线程阻塞的所有操作都必须使用异步实现！
- 你自测完毕后，确保新增的功能肯定没有任何问题后，要把测试时创建的测试脚本一类的文件删掉，别让项目里充斥着测试文件！
- 前端UI交互方面必须人性化，现代化
- UI设计必须现代化，加入大量的UI动效，让整个UI用起来灵动
- UI主题严禁使用蓝紫色渐变色！
- 软件前端尽可能不要使用Emoji作为图标使用！尽可能你自己直接编写配套的SVG文件作为图标用！
- 前端界面中的文本什么的必须得是中文的！除非是一些技术类词汇啥的可以是英文的！
- 开发的过程中注意更新一些requirements.txt一类的依赖项文件

- 所有Python库都安装在项目内置的Python3.13环境中
- Node.js库也是都安装在项目内置的Node.js环境中